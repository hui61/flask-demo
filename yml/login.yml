  This is user login API
    ---
  tags:
    - User login API
  parameters:
    - name: user_name
      in: 'formData'
      type: 'string'
      required: true
      description: The user name
    - name: user_password
      in: 'formData'
      type: 'string'
      required: true
      description: The User password
  responses:
    500:
      description: Fail
    200:
      description: Success